# 环境变量说明（本文件可提交 Git）
#
# 开发时通过 npm 脚本切换后端，接口路径始终是 /api/v1/...，无需改代码：
#   npm run dev:local   → 代理到 http://localhost:5000
#   npm run dev:remote  → 代理到 http://101.43.89.163:5000
#
# 对应配置文件（已提交）：.env.localhost / .env.remote
# 个人覆盖：复制为 .env.local 或 .env.[mode].local（见 .gitignore）
#
# DEV_API_PROXY_TARGET — 仅 Vite 开发代理使用（不含 /api/v1）
# VITE_API_BASE_URL     — 生产构建 / 浏览器直连时使用（不含 /api/v1）
