# 复制为 .env 后按需修改（.env 不会提交到 Git）

# 后端 API 根地址（不含 /api/v1，程序会自动拼接）
#
# 本地开发（推荐）：保持注释，前端请求 /api/v1/...，由 Vite 代理到 http://localhost:5141
# 直连联调 / 生产构建：取消注释并填写真实地址
# 仅开发代理目标（不含 /api/v1），不会注入前端。后端端口非 5000 时可设置：
# DEV_API_PROXY_TARGET=http://localhost:5141
